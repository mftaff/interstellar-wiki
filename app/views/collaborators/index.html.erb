<h4>Manage Collaborators for:</h4>
<h2><%= $markdown.render(@wiki.title).html_safe %></h2>

<table class="table">
    <thead>
        <th>User</th>
        <th></th>
    </thead>
    <tbody>
        <% @users.each do |user| %>
            <% unless user == @wiki.user %>
                <tr>
                    <td><%= user.username %></td>
                    <td>
                        <% if @wiki.users.include?(user) %>
                            <%= link_to "Remove as Collaborator", [@wiki, @wiki.collaborator_for(user)], method: :delete, class: "btn btn-danger" %>
                        <% else %>
                            <%= link_to "Add as Collaborator", wiki_collaborators_path(@wiki, user_id: user.id), method: :post, class: "btn btn-success" %>
                        <% end %>
                    </td>
                </tr>
            <% end %>
        <% end %>
    </tbody>
</table>