<% if current_user.admin? %>
    Signed in as: 
    <small><i>admin</i></small>
<% end %>
<% if current_user.premium? %>
    Signed in as: 
    <%= link_to image_tag("premium-icon.png", size: "21", alt: "Premium"), new_charge_path %>
<% end %>
<% if current_user.standard? %>
    <% unless request.env['PATH_INFO'] == new_charge_path %>
        <%= link_to 'Upgrade', new_charge_path, :class => 'navbar-link upgrade-btn' %>
    <% end %>
    Signed in as: 
<% end %>
