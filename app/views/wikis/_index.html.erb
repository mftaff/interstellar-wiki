<% wikis.each do |wiki| %>
    <% if (!wiki.private? || (current_user.admin? || current_user.id == wiki.user_id)) %>
        <%= render partial: 'privacy_icon', locals: {wiki: wiki} %>
        <%= link_to $markdown.render(wiki.title).html_safe, wiki %>
    <% end %>
<% end %>