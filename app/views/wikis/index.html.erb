<h3> Browse the collection.</h3>
<div class="row">
    <div class="col-md-8">
        <% @wikis.each do |wiki| %>
            <p><%= link_to wiki.title, wiki %></p>
        <% end %>
    </div>
</div>